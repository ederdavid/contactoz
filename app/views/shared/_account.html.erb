<% if current_user %>
<div class="account-info">
Bienvenido, <%= link_to  current_user.screen_name, current_user  %> (<a href="#" title="Data contributed:"><%=Action.count(:all, :conditions => ['user_id =?', current_user.id])%></a>) <a href="#" class="unrated"><% if !current_user.rating %> Unrated <%else%> <%= current_user.rating %> <%end%></a> <span>Points: <a href="#"><%=current_user.points%></a></span>
        </div>
<% end %>


