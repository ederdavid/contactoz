<% if current_user %>
<div class="account-info">
Welcome, <%= link_to  current_user.screen_name, current_user  %> (<a href="#" title="Data contributed:"><%=current_user.count(:action_id)%></a>) <a href="#" class="unrated"><% if !current_user.rating %> Unrated <%else%> <%= current_user.rating %> <%end%></a> <span>Points: <a href="#"><%=current_user.points%></a></span>
        </div>
<% end %>


