<h1>Listing display_companies</h1>

<table>
  <tr>
  </tr>

<% @display_companies.each do |display_company| %>
  <tr>
    <td><%= link_to 'Show', display_company %></td>
    <td><%= link_to 'Edit', edit_display_company_path(display_company) %></td>
    <td><%= link_to 'Destroy', display_company, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New display_company', new_display_company_path %>